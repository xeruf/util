ext.kotlinVersion = "1.2.40"

subprojects {
	apply plugin: "idea"
	idea {
		module {
			outputDir = file("build/idea/main")
			testOutputDir = file("build/idea/test")
		}
	}
	
	group = "xerus.util"
	repositories {
		mavenCentral()
	}
}
